const request = require('supertest');
const server = require('./server');

describe('server.js', () => {
  it('runs the tests', () => {
    expect(true).not.toBeFalsy();
  });//end run test
});//end server.js
